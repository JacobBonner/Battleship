package edu.colorado.objectgrind.enums;

/**
 * Represents the different statuses that a Location on a Grid can have.
 */
public enum LocationStatus {
    HIDDEN,
    HIT,
    MISS,
    FREE,
    OCCUPIED
}
